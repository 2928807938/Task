package com.task.domain.repository

import com.task.domain.model.task.requirementcompleteness.RequirementCompleteness

/**
 * 需求完整性仓储接口
 * 定义需求完整性相关的数据访问操作
 */
interface RequirementCompletenessRepository : Repository<RequirementCompleteness> {
} 