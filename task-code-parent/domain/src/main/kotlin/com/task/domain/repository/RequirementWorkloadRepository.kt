package com.task.domain.repository

import com.task.domain.model.task.requirementworkload.RequirementWorkload

/**
 * 需求工作量仓储接口
 * 定义需求工作量相关的数据访问操作
 */
interface RequirementWorkloadRepository : Repository<RequirementWorkload> {
} 